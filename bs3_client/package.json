{
  "name": "bs3_client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild --bundle src/index.ts --outdir=dist --sourcemap",
    "watch": "npm run build && onchange 'src/**/*.ts' -- npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "anymatch": "^3.1.1",
    "esbuild": "^0.7.22",
    "onchange": "^7.1.0",
    "rxjs": "^6.6.3",
    "rxjs-compat": "^6.6.3"
  }
}
